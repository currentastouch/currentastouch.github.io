# Demo Video

The original supplementary video is 224 MB, which exceeds GitHub's normal 100 MB single-file limit.

Before publishing the final page, place a compressed web version at:

```text
assets/videos/current-as-touch-demo-web.mp4
```

Alternatively, upload the original video to a GitHub Release, object storage, or another video host, then update the `<source>` URL in `index.html`.
